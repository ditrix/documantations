# Мультиагентний вайб-кодинг

**Мультиагентний вайб-кодинг** — це стиль розробки, за якого:

* код **не пишеться “одним мозком”**,
* а створюється через **набір спеціалізованих агентів**,
* кожен агент має *свою роль* (архітектор, backend, frontend, reviewer, devops тощо),
* а людина керує **напрямком і вайбом**, а не кожним рядком коду.

> Ключова ідея:
> **ти не кодиш — ти диригуєш.**

---

## Основні принципи

### 1. Розділення мислення, а не лише коду

Кожен агент:

* мислить **по-своєму**
* має **обмежений контекст**
* вирішує **вузьку задачу**

Приклад:

* Agent A — *архітектура*
* Agent B — *Laravel backend*
* Agent C — *Vue / TypeScript*
* Agent D — *безпека / edge cases*
* Agent E — *review / спрощення*

---

### 2. Асинхронність мислення

Агенти:

* не чекають один на одного
* можуть суперечити
* можуть пропонувати альтернативи

Ти:

* обираєш
* склеюєш
* спрощуєш

---

### 3. Vibe-driven development

Це не «best practices заради best practices».

Ти задаєш:

* стиль
* рівень строгості
* допустиму складність
* *“робимо MVP чи одразу прод?”*

Приклад вайбу:

> «Мінімум магії, читабельно, по-ларовелівськи, без overengineering»

---

### 4. Ітеративність

Не:

> «Одразу ідеальне рішення»

А:

1. грубий каркас
2. уточнення
3. спрощення
4. перевірка
5. автоматизація

---

## Типові ролі агентів

| Агент        | Що робить                     |
| ------------ | ----------------------------- |
| 🧠 Architect | Сутності, зв’язки, потоки     |
| ⚙️ Backend   | Контролери, сервіси, міграції |
| 🎨 Frontend  | UI, форми, валідація          |
| 🔐 Security  | XSS, CSRF, rate-limit         |
| 🧪 QA        | Edge cases, тести             |
| 🚀 DevOps    | Docker, npm, env, deploy      |

---

## Інструменти

### Оркестрація агентів

* **Cursor (multi-agent)**
* **Claude / ChatGPT з role prompting**
* **CrewAI**
* **AutoGen**
* **LangGraph**

Для твого стеку (Laravel + TS):

> 💡 *на практиці вистачає Cursor / ChatGPT + чітко заданих ролей*

---

### Управління знаннями

* `.md` файли
* `architecture.md`
* `agents.md`
* `rules.md`

---

## Методика роботи (практика)

### Крок 1. Формулювання задачі

```
Потрібно: блог з коментарями
Вимоги:
- гості читають
- авторизовані коментують
- захист від спаму
- мінімальний UI
```

---

### Крок 2. Роздача ролей (приклади промптів)

**Architect**

> Спроєктуй сутності та зв’язки для блогу з коментарями в Laravel.

**Backend**

> Реалізуй коментарі в Laravel 11: міграції, моделі, контролер.

**Frontend**

> Vue 3 + TS: форма коментаря з валідацією.

**Security**

> Знайди потенційні вразливості в реалізації коментарів.

---

### Крок 3. Зведення рішень

Ти:

* викидаєш зайве
* спрощуєш
* фіксуєш фінальний варіант

---

# Приклад проєкту: Блог з коментарями

## Архітектура

### Сутності

* `Post`
* `Comment`
* `User`

Зв’язки:

* Post hasMany Comment
* Comment belongsTo User
* Comment belongsTo Post

---

## Backend (Laravel)

### Міграція comments

```php
Schema::create('comments', function (Blueprint $table) {
    $table->id();
    $table->foreignId('post_id')->constrained()->cascadeOnDelete();
    $table->foreignId('user_id')->constrained()->cascadeOnDelete();
    $table->text('body');
    $table->timestamps();
});
```

---

### CommentController

```php
class CommentController extends Controller
{
    public function store(Request $request, Post $post)
    {
        $data = $request->validate([
            'body' => ['required', 'string', 'max:2000'],
        ]);

        $post->comments()->create([
            'body' => $data['body'],
            'user_id' => auth()->id(),
        ]);

        return back()->with('success', 'Comment added');
    }
}
```

---

### Захист

* `auth` middleware
* `ThrottleRequests`
* Blade: `{{ $comment->body }}` (escape!)

---

## Frontend (Vue + TypeScript)

```ts
<script setup lang="ts">
import { ref } from 'vue'

const body = ref('')
const error = ref('')

const submit = async () => {
  if (body.value.length < 3) {
    error.value = 'Занадто коротко'
    return
  }

  await fetch('/comments', {
    method: 'POST',
    headers: {
      'X-CSRF-TOKEN': document
        .querySelector('meta[name="csrf-token"]')!
        .getAttribute('content')!,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ body: body.value })
  })
}
</script>
```

---

## npm / package.json

```json
{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vue": "^3.4.0",
    "typescript": "^5.3.0"
  }
}
```

---

## vite.config.ts

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

---

## Підготовка та деплой

### .env (Laravel)

```
APP_ENV=production
APP_DEBUG=false
DB_CONNECTION=mysql
```

---

### Docker (за потреби)

```yml
services:
  app:
    build: .
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
```

---

### Продакшн-кроки

```bash
npm install
npm run build

composer install --no-dev
php artisan migrate --force
php artisan optimize
```

---

## Навчальні матеріали

### Концепція

* CrewAI docs
* AutoGen examples
* LangGraph (thinking flows)

### Практика

* role prompting
* agent debate
* critic agent

---

## Коли мультиагентний вайб-кодинг **дійсно корисний**

✅ архітектура
✅ складна бізнес-логіка
✅ pet-проєкти
✅ швидкі MVP
✅ рефакторинг legacy

🚫 дрібні правки
🚫 CRUD на одну таблицю

---

Якщо хочеш — наступним кроком можемо:

  * змоделювати **агентів саме під твій стек**
* зібрати **agents.md**
* або зробити **чат з ручним вибором агентів** (ти вже майже там 😉)

---

© 2026 ChatGPT (OpenAI).
Автор тексту: ChatGPT.


Круто, тоді робимо **модель агентів саме під твій стек**
(**Laravel + Vue 3 + TypeScript + Vite + LAMP / Docker**), без оверінженірингу — так, щоб це можна було **реально використовувати щодня**.

---

# Мультиагентна модель під твій стек

## Ціль

* швидко робити MVP
* не втрачати контроль над архітектурою
* тримати код читабельним
* працювати з legacy + новим кодом

---

## Базовий склад агентів (6 шт.)

```
/agents
  architect.md
  backend-laravel.md
  frontend-vue-ts.md
  security.md
  reviewer.md
  devops.md
```

Це **не AI-фреймворк**, а **контракти мислення**.

---

## 1. Architect Agent

### Роль

Проєктує структуру, але **не пише код**.

### Відповідальність

* доменні сутності
* зв’язки
* потоки даних
* що є sync / async
* де межі відповідальності

### Обмеження

* ❌ не пропонує конкретні бібліотеки
* ❌ не лізе в UI

### Prompt (шаблон)

```md
Ти — Architect Agent.
Стек: Laravel + Vue 3 + TypeScript.
Задача: {{task}}

Дай:
1. Сутності
2. Зв’язки
3. Потік даних (request → response)
4. Потенційні зони ризику

Без коду. Коротко.
```

---

## 2. Backend Agent (Laravel)

### Роль

Реалізує backend **laravel-way**.

### Відповідальність

* міграції
* моделі
* контролери
* сервіси (якщо потрібно)
* form request / validation

### Обмеження

* ❌ без overengineering
* ❌ без “універсальних” абстракцій

### Prompt

```md
Ти — Backend Laravel Agent.
Laravel 10/11, PHP 8+.

Реалізуй:
- міграції
- моделі
- контролер
- маршрути

Без зайвих сервісів.
Коментарі тільки там, де логіка неочевидна.
```

---

## 3. Frontend Agent (Vue 3 + TS)

### Роль

UI + інтеграція з backend.

### Відповідальність

* Vue 3 `<script setup>`
* TypeScript
* робота з формами
* обробка помилок

### Обмеження

* ❌ без UI-фреймворків (за замовчуванням)
* ❌ без state-management, якщо не треба

### Prompt

```md
Ти — Frontend Agent.
Vue 3 + TypeScript + Vite.

Зроби:
- компонент
- мінімальну валідацію
- fetch / axios
- обробку помилок

Код має бути простим і читабельним.
```

---

## 4. Security Agent

### Роль

Параноїк, який **ламає твоє рішення в голові**.

### Відповідальність

* XSS
* CSRF
* spam / flood
* доступи
* mass assignment

### Обмеження

* ❌ не переписує код
* ❌ не пропонує складні рішення

### Prompt

```md
Ти — Security Agent.

Проаналізуй рішення:
1. Потенційні уразливості
2. Реальні сценарії атаки
3. Мінімальні правки для захисту

Без фанатизму.
```

---

## 5. Reviewer Agent

### Роль

Той, хто каже:

> “Це можна простіше.”

### Відповідальність

* зменшення коду
* читабельність
* відповідність стилю Laravel

### Обмеження

* ❌ не додає фічі
* ❌ не міняє поведінку

### Prompt

```md
Ти — Code Reviewer.

Перевір:
- зайвий код
- складні конструкції
- дублювання

Запропонуй спрощення.
```

---

## 6. DevOps Agent

### Роль

Робить так, щоб **це запустилось**.

### Відповідальність

* npm
* build
* env
* docker / LAMP
* prod checklist

### Обмеження

* ❌ без Kubernetes
* ❌ без CI/CD, якщо не просять

### Prompt

```md
Ти — DevOps Agent.

Підготуй:
- npm scripts
- env приклади
- build + deploy steps

Орієнтир: простий хостинг або VPS.
```

---

# Як цим користуватись (реальний флоу)

## Приклад: “Коментарі в блозі”

1️⃣ **Architect**
→ визначає сутності

2️⃣ **Backend**
→ пише Laravel-код

3️⃣ **Frontend**
→ форма + інтеграція

4️⃣ **Security**
→ знаходить діри

5️⃣ **Reviewer**
→ спрощує

6️⃣ **DevOps**
→ готує деплой

> ❗ Ти **не запускаєш усіх одразу**
> Ти запускаєш **рівно того агента, який зараз потрібен**

---

## Правило «один агент — один результат»

❌

> “Зараз всі подумають”

✅

> “Architect → зафіксували → далі”

---

## Мінімальний `agents.md`

```md
# Agents rules

- Один агент = одна роль
- Агент не виходить за межі відповідальності
- Людина приймає фінальне рішення
- Якщо сумнів — обираємо простіше
```

---

## Коли ця модель ідеально лягає на твій досвід

З огляду на те, що ти:

* працюєш з Laravel давно
* маєш справу з legacy (PHP 5.6)
* любиш покроковий підхід
* робиш pet-проєкти + MVP

👉 **це майже “екзоскелет для мислення”**, а не автокодер.
