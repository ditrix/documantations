# Что такое мультиагентный вайб-кодинг

**Мультиагентный вайб-кодинг** — это стиль разработки, при котором:

* код **не пишется “одним мозгом”**,
* а создаётся через **набор специализированных агентов**,
* каждый агент отвечает за *свою роль* (архитектор, backend, frontend, reviewer, devops и т.д.),
* а человек управляет **направлением и вайбом**, а не каждой строкой.

> Ключевая идея:
> **ты не кодишь — ты дирижируешь.**

---

## Основные принципы

### 1. Разделение мышления, а не только кода

Каждый агент:

* думает **по-своему**
* имеет **ограниченный контекст**
* решает **узкую задачу**

Пример:

* Agent A — *архитектура*
* Agent B — *Laravel backend*
* Agent C — *Vue / TS*
* Agent D — *security / edge cases*
* Agent E — *review / упрощение*

---

### 2. Асинхронность мышления

Агенты:

* не ждут друг друга
* могут противоречить
* могут предлагать альтернативы

Ты:

* выбираешь
* склеиваешь
* упрощаешь

---

### 3. Vibe-driven development

Это не «best practices ради best practices».

Ты задаёшь:

* стиль
* уровень строгости
* допустимую сложность
* *“делаем MVP или сразу прод?”*

Пример вайба:

> «Минимум магии, читаемо, Laravel-way, без overengineering»

---

### 4. Итеративность

Не:

> «Сразу идеальное решение»

А:

1. грубый скелет
2. уточнение
3. упрощение
4. проверка
5. автоматизация

---

## Типовые роли агентов

| Агент        | Что делает                     |
| ------------ | ------------------------------ |
| 🧠 Architect | Сущности, связи, потоки        |
| ⚙️ Backend   | Контроллеры, сервисы, миграции |
| 🎨 Frontend  | UI, формы, валидация           |
| 🔐 Security  | XSS, CSRF, rate-limit          |
| 🧪 QA        | Edge cases, тесты              |
| 🚀 DevOps    | Docker, npm, env, deploy       |

---

## Инструменты

### Оркестрация агентов

* **Cursor (multi-agent)**
* **Claude / ChatGPT с role prompting**
* **CrewAI**
* **AutoGen**
* **LangGraph**

Для твоего стека (Laravel + TS):

> 💡 *реально хватает Cursor / ChatGPT + строгих ролей*

---

### Управление знаниями

* `.md` файлы
* `architecture.md`
* `agents.md`
* `rules.md`

---

## Методика работы (практика)

### Шаг 1. Формулировка задачи

```
Нужно: блог с комментариями
Требования:
- гости читают
- авторизованные комментируют
- защита от спама
- минимальный UI
```

---

### Шаг 2. Раздача ролей (пример промптов)

**Architect**

> Спроектируй сущности и связи для блога с комментариями в Laravel.

**Backend**

> Реализуй комментарии в Laravel 11: миграции, модели, контроллер.

**Frontend**

> Vue 3 + TS: форма комментария с валидацией.

**Security**

> Найди уязвимости в комментариях.

---

### Шаг 3. Сведение решений

Ты:

* выбрасываешь лишнее
* упрощаешь
* фиксируешь финальный вариант

---

# Пример проекта: Блог с комментариями

## Архитектура

### Сущности

* `Post`
* `Comment`
* `User`

Связи:

* Post hasMany Comment
* Comment belongsTo User
* Comment belongsTo Post

---

## Backend (Laravel)

### Миграция comments

```php
Schema::create('comments', function (Blueprint $table) {
    $table->id();
    $table->foreignId('post_id')->constrained()->cascadeOnDelete();
    $table->foreignId('user_id')->constrained()->cascadeOnDelete();
    $table->text('body');
    $table->timestamps();
});
```

---

### CommentController

```php
class CommentController extends Controller
{
    public function store(Request $request, Post $post)
    {
        $data = $request->validate([
            'body' => ['required', 'string', 'max:2000'],
        ]);

        $post->comments()->create([
            'body' => $data['body'],
            'user_id' => auth()->id(),
        ]);

        return back()->with('success', 'Comment added');
    }
}
```

---

### Защита

* `auth` middleware
* `ThrottleRequests`
* Blade: `{{ $comment->body }}` (escape!)

---

## Frontend (Vue + TS)

```ts
<script setup lang="ts">
import { ref } from 'vue'

const body = ref('')
const error = ref('')

const submit = async () => {
  if (body.value.length < 3) {
    error.value = 'Too short'
    return
  }

  await fetch('/comments', {
    method: 'POST',
    headers: {
      'X-CSRF-TOKEN': document
        .querySelector('meta[name="csrf-token"]')!
        .getAttribute('content')!,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ body: body.value })
  })
}
</script>
```

---

## npm / package.json

```json
{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vue": "^3.4.0",
    "typescript": "^5.3.0"
  }
}
```

---

## vite.config.ts

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

---

## Подготовка и деплой

### .env (Laravel)

```
APP_ENV=production
APP_DEBUG=false
DB_CONNECTION=mysql
```

---

### Docker (по желанию)

```yml
services:
  app:
    build: .
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
```

---

### Продакшн шаги

```bash
npm install
npm run build

composer install --no-dev
php artisan migrate --force
php artisan optimize
```

---

## Учебные материалы

### Концепция

* CrewAI docs
* AutoGen examples
* LangGraph (thinking flows)

### Практика

* “Role prompting”
* “Agent debate”
* “Critic agent”

---

## Когда мультиагентный вайб-кодинг **реально полезен**

✅ архитектура
✅ сложная бизнес-логика
✅ pet-проекты
✅ быстрые MVP
✅ рефакторинг legacy

🚫 мелкие правки
🚫 CRUD на 1 таблицу

---

Если хочешь — в следующем шаге можем:

* смоделировать **агентов именно под твой стек**
* собрать **agents.md**
* или сделать **чат с ручным выбором агентов** (ты уже к этому близок 😉)
